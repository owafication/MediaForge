<Window x:Class="MediaForge.PresetEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preset" Width="560" Height="430" MinWidth="500" MinHeight="380"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name" VerticalAlignment="Center"/>
        <TextBox x:Name="NameText" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Group" VerticalAlignment="Center"/>
        <TextBox x:Name="GroupText" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Description" VerticalAlignment="Top"/>
        <TextBox x:Name="DescriptionText" Grid.Row="2" Grid.Column="1" Height="72" AcceptsReturn="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
            <TextBlock Text="Locked field keys" FontWeight="SemiBold"/>
            <TextBlock Margin="0,3,0,6" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"
                       Text="Comma-separated typed setting names. Locked fields cannot be changed by per-job overrides until this user preset is edited or duplicated."/>
            <TextBox x:Name="LockedFieldsText" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" MinHeight="100"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" IsCancel="True"/>
            <Button Content="Save" IsDefault="True" Style="{StaticResource PrimaryButton}" Click="Save_Click" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
